# Speed up installs by using the global cache
--prefer-offline true

# Don't generate yarn.lock changes for optional dependencies you don't need
--ignore-optional false

# Save exact versions (like npm --save-exact)
--save-exact true

# Enable colorful output in CI logs
--color always
